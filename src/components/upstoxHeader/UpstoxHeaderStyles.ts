import {StyleSheet} from 'react-native';
import {colors} from '../../theme/Colors';

export const upstoxHeaderStyles = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: colors.primary,
  },
  titleText: {
    color: colors.white,
    fontWeight: 'bold',
  },
});
